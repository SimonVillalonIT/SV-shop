/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["placehold.co", "images.asos-media.com"],
        dangerouslyAllowSVG: true,
    }
};

export default nextConfig;
